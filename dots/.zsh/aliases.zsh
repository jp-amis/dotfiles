#!/bin/bash
alias hosts='sudo vim /etc/hosts'
alias cleanup_dsstore="find . -name '*.DS_Store' -type f -ls -delete"

alias cd-storage="cd /Volumes/Storage"

alias vim="mvim -v"
alias vi="mvim -v"

alias cat="bat"
